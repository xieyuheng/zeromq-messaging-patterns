{
  "private": true,
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "rm -rf lib && npx tsc",
    "build:watch": "rm -rf lib && npx tsc --watch",
    "format": "prettier src --write"
  },
  "dependencies": {
    "@vue/runtime-core": "^3.2.47",
    "picocolors": "^1.0.0",
    "zeromq": "^6.0.0-beta.16"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "prettier": "^2.8.4",
    "prettier-plugin-organize-imports": "^3.2.2",
    "typescript": "^4.9.5"
  }
}
